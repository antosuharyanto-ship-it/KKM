{
    "name": "kkm-monorepo-root",
    "version": "1.0.0",
    "private": true,
    "description": "Root proxy for Railway deployment",
    "scripts": {
        "postinstall": "cd server && npm install",
        "build": "cd server && npm run build",
        "start": "cd server && npm start"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}